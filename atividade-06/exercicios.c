/*
QUESTOES ABERTAS DA LISTA 6

1.
Primeiro Exemplo: Pilha de aplicativos abertos no android.
Segundo Exemplo: Alt + Tab (atalho de troca de janelas do windows)


2. Na pilha, a inserção de um dado é realizado jogando o dado para o topo, utilizando somente um
ponteiro, chamado TOP. Com isso, os dados vão se EMPILHANDO, um em cima do outro.
    Para a remoção dos dados, eles são removidos utilizando o mesmo ponteiro TOP, então, os últimos
dados colocados serão os primeiros a serem removidos, denominando assim o nome do método LIFO (Last
In, First Out).
    Já na fila, o método utilizado é o FIFO (First In, First Out), ou seja, o primeiro que entra
na Fila também é o primeiro a sair. A inserção dos dados é realizado com um ponteiro chamado FIM e
a remoção é feita com um ponteiro chamado INICIO.


4. O tipo de ponteiro deve ser do tipo do próprio nó, criando um ponteiro que aponta para si mesmo.
Já no consumo de memória, vai depender da arquitetura do computador. Se for um sistema 32 bits, ele
consumirá 4 bytes de memória, e, se for um sistema 64 bits, ele consumirá 8 bytes de memória.


5. 
- Push (Empilhar): Insere o dado no topo da pilha
- Pop (Desempilhar): Remove o dado do topo da pilha


6. Vai identificar que a pilha está vazia, pois, se o ponteiro que aponta para o topo está com o 
valor NULL, então significa que não tem dado inserido.

13. R: 15

17. AA

*/